---
description: Run a comprehensive post-development security audit of a Catalyst by Zoho project (security, all 15 components, scalability, and last 30 days of commits).
argument-hint: "[path/to/catalyst-project]"
---

Read `${CLAUDE_PLUGIN_ROOT}/skills/catalyst-security-audit/SKILL.md` and follow its instructions exactly.

Project path: use `$ARGUMENTS` if provided, otherwise the current working directory.

`${CLAUDE_PLUGIN_ROOT}` resolves to this plugin's root, which contains `skills/`, `phases/`, and `components/` as siblings.
